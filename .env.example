# ==============================================================================
# Example environment variables for local development
# Copy this file to `.env` and fill the values (do NOT commit the real .env file)
# ==============================================================================

# --- PUBLIC CLIENT-SIDE VARIABLES (safe for frontend) ---
# These are intentionally exposed to the client and should only contain PUBLIC keys.
# Vite exposes only variables prefixed with VITE_ to the client bundle.

# Supabase public URL and anon key (these are designed to be public)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-anon-key

# Optional: local feature flags or overrides
# VITE_FEATURE_FLAG_X=true

# ==============================================================================
# IMPORTANT: SERVER-SIDE SECRETS (NEVER expose these in frontend code)
# ==============================================================================
# The following secrets should ONLY be configured in Supabase Edge Functions
# via the Supabase Dashboard → Edge Functions → Manage secrets
#
# DO NOT add these to this file or any .env file in the repository:
#
# - SUPABASE_SERVICE_ROLE_KEY  (server admin access)
# - BREVO_API_KEY              (email service)
# - BREVO_REFERRAL_TEMPLATE_ID (email template ID)
# - BREVO_SENDER_EMAIL         (email sender)
# - BREVO_SENDER_NAME          (email sender name)
# - REFERRAL_SIGNUP_URL        (signup URL for referrals)
# - OPENAI_API_KEY             (if using AI features)
# - STRIPE_SECRET_KEY          (if using payments)
# - ELEVENLABS_API_KEY         (if using voice features)
#
# These secrets are managed server-side in Supabase Edge Functions.
# See: https://supabase.com/docs/guides/functions/secrets
# ==============================================================================
